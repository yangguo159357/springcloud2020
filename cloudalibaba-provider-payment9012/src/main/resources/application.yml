server:
  port: 9012

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848 #配置Nacos地址
        server-addr: 192.168.10.101:1111 #配置Nacos集群地址，先将地址给nginx,让其进行转发

management:
  endpoints:
    web:
      exposure:
        include: '*'




